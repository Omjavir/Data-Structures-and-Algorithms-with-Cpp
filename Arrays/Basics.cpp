#include <iostream>
#include <climits>
using namespace std;
int main()
{
    // int array[3] = {23, 534, 15};
    // cout << array[0];
    // cout << array[1];
    // cout << array[2];

    // // Taking the length of array and objects of array as input and printing the objects of array
    // int n;
    // cin >> n;
    // int array[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> array[i];
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     cout << array[i] << " ";
    // }

    // // Taking the length of array and objects of array as input and printing the objects of array and checking maximum and minimum number.
    // int n;
    // cin >> n;
    // int array[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> array[i];
    // }
    // int maxNo = INT_MIN;
    // int minNo = INT_MAX;
    // for (int i = 0; i < n; i++)
    // {
    //     maxNo = max(maxNo, array[i]);
    //     minNo = min(minNo, array[i]);
    // }
    // cout << maxNo << " " << minNo << endl;
    // return 0;

    // // Checking for the key in object and returning the index of key

    
    return 0;
}