#include <iostream>
#include <algorithm>
#include <string>
using namespace std;
int main()
{
    string str;
    // // Input
    // cin >> str;
    // // Output
    // cout << str;
    // To get a sentence as input
    // getline(cin, str);
    // cout << str;

    // // Accessing a particular character from a string
    // str = "Cricket";
    // cout << str[3] << endl;

    // Append two string
    // string str1 = "Fam";
    // string str2 = "ily";
    // string str3;
    // str3 = str1 + str2;
    // cout << str1 + str2 << endl;
    // str1.append(str2);
    // cout << str1 << endl;
    // cout << str3 << endl;

    // // String to integer
    // str = "9320";
    // int x = stoi(str);
    // cout << x << endl;

    // // Integer to string
    // int x = 9320;
    // str = to_string(x);
    // cout << str << endl;

    // // Sorting a string
    // str = "sgrksjhfkjsao";
    // sort(str.begin(), str.end());
    // cout << str << endl;

    // Changing the whole string to uppercase or lowercase
    // str = "SGsdfdaoRKsdFdfsJSAO";
    // For UpperCase
    // for (int i = 0; i < str.length(); i++)
    // {
    //     if (str[i] >= 'a' && str[i] <= 'z')
    //     {
    //         str[i] -= 32;
    //     }
    // }

    // // For LowerCase
    // for (int i = 0; i < str.length(); i++)
    // {
    //     if (str[i] >= 'A' && str[i] <= 'Z')
    //     {
    //         str[i] += 32;
    //     }
    // }
    // // Inbuilt function to convert to Uppercase || Lowercase
    // transform(str.begin(), str.end(), str.begin(), ::toupper);
    // transform(str.begin(), str.end(), str.begin(), ::tolower);
    // cout << str << endl;

    // // Sorting in number of string array in decreasing order
    // string n;
    // cin >> n;
    // sort(n.begin(), n.end());
    // for (int i = n.length(); i >= 0; i--)
    // {
    //     cout << n[i];
    // }
    return 0;
}